Time Complexity isO(n) and space complexity is O(n)
class Solution {
    public int minSubarray(int[] nums, int p) {
        int len =nums.length;
        int sum =0,curr=0;
        for(int num:nums){
            sum = (sum+num)%p;
        }
        int target = sum%p;
        if(target ==0){
            return 0;
        }
        int result =len;
        HashMap<Integer,Integer> map = new HashMap<>();
        map.put(0,-1);
        for(int i=0;i<len;i++){
            curr = (curr + nums[i])%p;
            int remain = (curr-target+p)%p;
            if(map.containsKey(remain)){
                 result =Math.min(result,i-map.get(remain));

            }
            map.put(curr,i);
        }
        return result==len? -1:result;

    }
}